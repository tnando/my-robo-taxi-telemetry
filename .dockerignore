# Version control
.git
.gitignore
.github

# Claude agent memory and worktrees
.claude

# TLS certificates — never bake secrets into the image
certs/

# Documentation
*.md
docs/

# Test infrastructure — not needed in runtime image.
# Note: *_test.go files are kept in the build context so
# docker-compose.test.yml can run tests from the builder stage.
tests/

# Local development artifacts
bin/
telemetry-server

# Editor and OS noise
.DS_Store
*.swp
*.swo
.idea/
.vscode/
